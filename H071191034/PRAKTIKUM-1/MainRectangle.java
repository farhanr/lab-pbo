class MainRectangle {
    public static void main(String[] args) {
        Rectangle rec = new Rectangle();
        rec.setHeight(30);
        rec.setWidth(5);
        rec.desc();
        // rec.height = 30;
        // rec.width = 5;
        // System.out.println("Luas = "+ rec.getArea());
    }
}